Upload catalog image for book.

<%= form_tag( url: book_book_catalogs_path(book), method: :post ) do  %>
  <%= label_tag :catalog %>
  <%= file_field_tag :catalog, multipart: true %>

  <%= submit_tag "Upload", class: "btn btn-large btn-primary" %>
<% end %>
